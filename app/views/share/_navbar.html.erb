<nav class="navbar navbar-default navbar-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%= image_tag("logo.png", :width =>'80') %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <%= link_to '成為教練', users_path, :style=>"color:black", class: :'pull-right' %>

      <ul class="nav navbar-nav navbar-right">

        <% if current_user %>
            <li class="dropdown">
              <%#= link_to current_user.name, user_path(current_user) %>
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" ><b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to('編輯個人資料', edit_registration_path(:user),  :style=>"color:black") %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to('登出', destroy_user_session_path, :method => :delete, :style=>"color:black") %></li>
        <% else %>
                <li><%= link_to('瀏覽', new_registration_path(:user), :style=>"color:black") %></li>

                <li><%= link_to('註冊', new_registration_path(:user), :style=>"color:black") %></li>
                <li><%= link_to('登入', new_session_path(:user), :style=>"color:black")%></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>